# 前端说明

当前为了保证本地零额外依赖即可运行，管理台已经直接集成到：

`/Users/zhaox/IdeaProjects/aigc-content-factory/aigc-content-factory-server/src/main/resources/static`

因此启动后端后，直接访问 [http://localhost:8080](http://localhost:8080) 即可使用完整前端。

这个目录保留为后续拆分独立前端工程的预留位置。
